<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="toolbar_expanded_text">
        <item name="android:textSize">@dimen/toolbar_expanded_title_size</item>
    </style>

    <style name="toolbar_collapsed_text">
        <item name="android:textSize">@dimen/toolbar_collapsed_title_size</item>
    </style>

</resources>
